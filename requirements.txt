# Streamlit アプリ・文字起こし（WhisperX）用
# Main file: app.py
#
# torch / torchaudio は whisperx が依存関係として解決するため、ここでは明示しない
# （バージョンを固定しすぎると pip の解決失敗や Streamlit Cloud のビルド失敗の原因になります）
streamlit>=1.36.0
python-dotenv>=1.0.0
openai>=1.0.0
yt-dlp>=2024.1.0

# PyPI の安定版に固定（解決の再現性を上げる）
whisperx==3.2.0
